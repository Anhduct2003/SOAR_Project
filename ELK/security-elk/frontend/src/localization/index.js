import {
  apiMessageKeyMap,
  DEFAULT_LOCALE,
  LOCALE_LABELS,
  SUPPORTED_LOCALES
} from './config';
import en from './locales/en';
import vi from './locales/vi';

export {
  apiMessageKeyMap,
  DEFAULT_LOCALE,
  LOCALE_LABELS,
  SUPPORTED_LOCALES
};

export const translations = {
  en,
  vi
};
